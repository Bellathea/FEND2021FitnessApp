import React from 'react';

const program = (props) => {
    return (
        <div>
            <p>{props.workoutTitle}</p>
            <p>{props.programTitle}</p>
        </div>   
    )
};

export default program;